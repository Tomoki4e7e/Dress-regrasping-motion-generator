# 開発フローの構想

1. データ収集
    - 足の角度、靴下のたわみ方、初期の被覆率を変更する
    - 靴下を足先に被せた状態からグリッパーで様々な掴み方で靴下をはかせるデータを収集する
    - 視覚情報
    - 関節トルク
    - グリッパーの手先の触覚情報
    - 既存のモデルで模倣学習を行う
2. データの前処理
    1. 視覚情報
        - 靴下領域、足領域の深度情報の抽出
        - 靴下領域/足領域から被覆率を計算←新規
        - 靴下の開口部の縁6〜8点、かかと・つま先推定位置←新規
        - 足のつま先、かかとなどの基準点から足の長さを計算し、把持点の位置を正規化する←新規
    2. 触覚情報
        - グリッパーの先に加わる垂直抗力、せん断力ベクトルを計算しておく
3. 模倣学習
    - **Close-Fitting Dressing Assistance Based on State Estimation of Feet and Garments with Semantic-based Visual Attention (2025)**   のモデルに、前処理済みデータを追加して学習する
4. 強化学習
    - 模倣学習モデルのベースポリシーを補正する残差ポリシーを学習する
    - ゴール条件付きオフライン残差強化学習
    - Hindsight Experience Replay (HER) により、軌道の途中にサブゴールが自動で設定される
    - 状態(s), 行動(a)に加えてゴール条件(g)を入力に与えることで、異なる軌道の断片を暗黙的に組み合わせて、データセットに存在しない新しい軌道を生成する
    - 報酬
        - 被覆率、把持点の進捗から報酬を計算する
        - グリッパーの手先に加わる力が極端に大きくなった場合にペナルティを与える